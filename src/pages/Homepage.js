import Banner from "../components/Banner/Banner";
import React from "react";

const Homepage = () => {
  return (
    <section>
      <section id="slider">
        <Banner />
      </section>
    </section>
  );
};

export default Homepage;
