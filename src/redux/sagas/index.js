import { takeLatest, call, put } from 'redux-saga/effects';
import * as actions from '../actions';
// import * as api from '../../api';
// saga để tương tác với database

function* mySaga() {
 
}

// generator function ES6

export default mySaga;